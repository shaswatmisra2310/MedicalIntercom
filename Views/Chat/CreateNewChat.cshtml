@model NewChatModel
@if(ViewBag.currentUser=="Admin")
@Html.Partial("_Index")
else
@Html.Partial("_Index_user")
@using (Html.BeginForm("startchat","Chat"))
{

    <input type="text" name="chattopic">
    
    <input type="submit" name="create chat">
}